---

- hosts: all
  become: yes
  gather_facts: yes
  tasks:
    - name: Additional groups for default user
      user:
        name: "{{ default_user }}"
        groups: wheel,audio
        append: yes
      when: ansible_os_family == "Archlinux"
