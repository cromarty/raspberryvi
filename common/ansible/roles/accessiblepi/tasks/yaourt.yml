---
## yaourt.yml

- name: Arch, install package-query
  aur:
    name: package-query
    user: "{{ default_user }}"
    dir: /tmp
  tags:
    - yaourt

- name: Arch, install yaourt
  aur:
    name: yaourt
    user: "{{ default_user }}"
    dir: /tmp
  tags:
    - yaourt
